# Example only — create real secret before production deploy:
# kubectl create secret generic element-secrets --from-literal=jwt-secret='replace-with-64-char-secret'
apiVersion: v1
kind: Secret
metadata:
  name: element-secrets
type: Opaque
stringData:
  jwt-secret: "REPLACE_ME_WITH_A_LONG_RANDOM_SECRET"
